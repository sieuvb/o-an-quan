version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    ports:
      - "3000:3000"
    ports:
      - "3003:3003"
